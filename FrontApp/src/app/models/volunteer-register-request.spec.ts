import { VolunteerRegisterRequest } from './volunteer-register-request';

describe('VolunteerRegisterRequest', () => {
  it('should create an instance', () => {
    expect(new VolunteerRegisterRequest()).toBeTruthy();
  });
});
