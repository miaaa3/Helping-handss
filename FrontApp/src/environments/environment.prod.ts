// Production environment config. Swapped in automatically for `ng build --configuration=production`
// via the `fileReplacements` entry in angular.json.
//
// Update `apiUrl` to your deployed backend's base URL (must end with '/').
export const environment = {
  apiUrl: '/',
  enableDebug: false
};
