server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Reasonable upload size for post media.
    client_max_body_size 20m;

    # --- Backend REST endpoints ---
    location /api/     { proxy_pass http://backend:8080; include /etc/nginx/proxy_common.conf; }
    location /auth     { proxy_pass http://backend:8080; include /etc/nginx/proxy_common.conf; }
    location /public/  { proxy_pass http://backend:8080; include /etc/nginx/proxy_common.conf; }
    location /uploads/ { proxy_pass http://backend:8080; include /etc/nginx/proxy_common.conf; }

    # --- WebSocket / SockJS (STOMP chat) ---
    location /ws {
        proxy_pass http://backend:8080;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_read_timeout 3600s;
    }

    # --- Angular SPA: everything else falls back to index.html ---
    location / {
        try_files $uri $uri/ /index.html;
    }
}
